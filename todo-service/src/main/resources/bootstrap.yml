# App
spring:
  application:
    name: todo-service

  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-service
      fail-fast: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/